name = "gni"
main = "src/index.ts"
compatibility_date = "2026-08-01"
compatibility_flags = ["nodejs_compat"]

# Sem segredos aqui: DATABASE_URL entra como secret (wrangler secret put
# DATABASE_URL, ou dashboard → Settings → Variables → Secrets).
[observability]
enabled = true
